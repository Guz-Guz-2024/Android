<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base para Dos_Login -->
    <style name="Base.Theme.Dos_Login" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <!-- Tema principal para Dos_Login -->
    <style name="Theme.Dos_Login" parent="Base.Theme.Dos_Login">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Color del texto en la Toolbar -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>

        <!-- Configuración de la Toolbar -->
        <item name="toolbarStyle">@style/CustomToolbarStyle</item>
    </style>

    <style name="CustomToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="titleTextColor">@color/black</item> <!-- Título -->
        <item name="subtitleTextColor">@color/black</item> <!-- Subtítulo (si aplica) -->
        <item name="android:textColorPrimary">@color/black</item> <!-- Texto primario -->
        <item name="android:textColorSecondary">@color/black</item> <!-- Texto secundario -->
        <item name="android:colorControlNormal">@color/black</item> <!-- Iconos -->
    </style>



    <!-- Estilo personalizado para la Toolbar -->
    <style name="CustomToolbarStyle" parent="Widget.Material3.Toolbar">
        <item name="titleTextColor">@color/black</item> <!-- Color del título -->
        <item name="subtitleTextColor">@color/black</item> <!-- Color del subtítulo -->
    </style>
</resources>
